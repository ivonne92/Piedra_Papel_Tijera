<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.doris.piedra_papel_tijera" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".Principal"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".NuevoJugador"
            android:label="@string/title_activity_nuevo_jugador" >
        </activity>
        <activity
            android:name=".IniciarJuego"
            android:label="@string/title_activity_iniciar_juego" >
        </activity>
        <activity
            android:name=".Puntajes"
            android:label="@string/title_activity_puntajes" >
        </activity>
    </application>

</manifest>
